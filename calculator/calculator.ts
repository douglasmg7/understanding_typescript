
// Define a type not a value (objec)
type InvestmentData = {
    initialAmount: number,
    annualContribution: number,
    expetedReturn: number,
    duration: number,
};

function calculateInvestment(data: InvestmentData) {}
